WebAssembly is a portable binary execution format for programs. It is extremely used in the browser.

WebAssembly is designed to be embedded into systems, and it will be executed in a secure sandbox that cannot be escaped, which means that the code that runs there will be completely controlled by the outside system.
